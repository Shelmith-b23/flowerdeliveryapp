from setuptools import setup, find_packages

setup(
    name="flower-delivery-app",
    version="1.0.0",
    description="Flower delivery application with Flask backend",
    author="Your Name",
    packages=find_packages(),
    python_requires=">=3.8",
    include_package_data=True,
)
